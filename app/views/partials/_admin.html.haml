- if session[:user_id] 
  #admin
    
    %span.admin_link= link_to t(:orders), orders_path
    - if can? :manage, @users
      %span.admin_link= link_to t(:users), users_path
    - if can? :manage, @categories
      %span.admin_link= link_to t(:categories), categories_path
    - if can? :manage, @products
      %span.admin_link= link_to t(:products), products_path
    - if can? :manage, @news_items
      %span.admin_link= link_to t(:news_items), news_items_path
    - if can? :manage, @partners
      %span.admin_link= link_to t(:partners), partners_path
    - if can? :manage, @brands
      %span.admin_link= link_to t(:brands), brands_path  
    
    .admin_link.logout= link_to t(:logout), logout_path    



