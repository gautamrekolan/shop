= form_for @product_image do |f|
  -if @product_image.errors.any?
    #error_explanation
      %h2= "#{pluralize(@product_image.errors.count, "error")} prohibited this product_image from being saved:"
      %ul
        - @product_image.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :image
    = f.text_field :image
  .actions
    = f.submit 'Save'
