development:
  host: localhost
  database: shop_development

test:
  host: localhost
  database: shop_test

# set these environment variables on your prod server
production:
  uri: <%= ENV['MONGOHQ_URL'] %>
